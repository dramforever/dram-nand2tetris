CHIP ALUStat {
    IN in[16];
    OUT zr, ng;

    PARTS:

    Or(a=in[15], b=false, out=ng);

    Or8Way(in=in[0..7], out=nz1);
    Or8Way(in=in[8..15], out=nz2);
    Or(a=nz1, b=nz2, out=nz);
    Not(in=nz, out=zr);
}
